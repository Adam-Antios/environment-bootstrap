# README

This explains how to create a devcontainer for an existing project.

1. Create the required directory structure, such as `.directory` and `Directory`, for the mounted volumes in `devcontainer.json`.

1. Select `Reopen in Container` to rebuild and start the development container with the mounts applied.
